[project]
name = "core-contracts"
version = "0.1.0"
description = "Core domain models for HoneyTrade Equities"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
