[project]
name = "broker-adapters"
version = "0.1.0"
dependencies = [
    "core-contracts",
    "httpx",
    "pydantic"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
